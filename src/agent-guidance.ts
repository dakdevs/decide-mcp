export const agentInstructions = `Use evaluate for focused evidence checks, classification, candidate selection, and rubric assessments. Use decide for a single recommendation among supplied choices. Supply only relevant context and batch independent questions that share it. Questions in one batch cannot read each other's answers. Question IDs are labels; write complete instructions and concrete, self-contained criteria. Boolean returns P(true), not degree; use score for degree. Score levels are zero-based descriptive positions, not arbitrary numbers. Include unknown or none-applies options when appropriate. Keep arithmetic, extended reasoning, and explanations in the calling agent or code. Provider confidence measures distribution concentration, not probability of correctness. Results are advisory and do not grant permission to act. Do not retry merely to obtain a preferred answer; gather evidence or refine an ambiguous question. Evaluate uses the default policy unless an explicit profile is selected; decide may route automatically.`;

export const evaluateDescription =
  "Evaluate 1–64 independent typed questions against shared state in one batch: boolean checks return P(true); choice selects from 2–255 alternatives; score rates against 2–10 ordered descriptive levels. Use for verification, classification, relevance, severity, and candidate assessment. Returns distributions, separate provider confidence when available, source labels, rubric levels, and model details. No automatic profile routing. Questions cannot depend on other answers in the same call.";
